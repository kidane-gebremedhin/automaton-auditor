"""Context builder: load rubric, prepare inputs for detectives."""

from src.state import AgentState


def context_builder(state: AgentState) -> dict:
    """Load rubric, validate inputs, prepare context for detectives.

    TODO: Load rubric from file, parse repo_url and pdf_path, set rubric_dimensions.
    """
    # Placeholder: pass through
    return {}
